# 胡文轩毕业答辩 PPT 逐页演讲稿

> 依据当前文件 `C:\Users\轩哥\Desktop\胡文轩中期PPT.pptx` 的 18 页内容，并结合论文 `main.tex`、摘要与结论整理。建议总时长控制在 10-12 分钟；正式答辩前可按个人语速再压缩或扩展 1 分钟左右。

## Slide 1 封面（约 20 秒）

各位评委老师，大家好。我是胡文轩，今天汇报的论文题目是《面向立体仓库堆叠货物盘点任务的改进实例分割与模型压缩方法》。本课题主要面向立体仓库中堆叠货物的自动化盘点问题，围绕实例分割、数量推理以及边缘部署模型压缩三个方面展开研究。下面我将从研究背景、研究方法、实验结果以及总结展望四个部分进行汇报。

## Slide 2 研究背景（约 45 秒）

首先介绍研究背景。库存盘点是现代仓储管理中的关键环节，它直接影响库存控制、作业成本和运行效率。传统方法通常依赖人工、RFID 或传感器等方式，虽然可以完成盘点任务，但存在人工依赖强、标签成本高、系统维护复杂等问题。  

随着计算机视觉的发展，基于视觉的自动盘点成为一种更低成本、更灵活的方案。对于立体仓库来说，现场可以通过摄像头采集堆叠货物图像，再由边缘服务器完成识别和计数，并将结果返回管理系统。但这里有两个核心挑战：一是堆叠货物存在遮挡、边界粘连和层次结构复杂的问题，导致现有视觉方法精度不足；二是边缘服务器计算资源有限，需要对模型进行轻量化压缩。因此，本文的研究重点就是提升视觉盘点的准确性和实时性。

## Slide 3 研究内容（约 35 秒）

本论文的研究目标，是提高立体仓库系统中堆叠货物盘点的准确性和实时性。围绕这一目标，本文主要完成了三项工作。  

第一，针对堆叠场景中遮挡严重、纹理重复、边界粘连的问题，提出改进实例分割模型 YOLO11-StackLite。第二，针对仅有检测结果难以直接推断库存总数的问题，构建了基于面积比的后处理计数流程，实现从实例分割结果到盘点数量的转换。第三，针对边缘服务器部署资源受限的问题，设计了结构化剪枝与知识蒸馏相结合的轻量化压缩路线。后续内容也会按照这三条主线展开。

## Slide 4 现有实例分割算法存在的问题（约 35 秒）

这里展示的是现有 YOLO11s-Seg 与后处理模块结合进行货物统计的基本思路。YOLO11s-Seg 本身具备较好的实时分割能力，但直接应用到立体仓库堆叠货物盘点任务时，仍然存在检测精度不足的问题。  

原因主要有三点：第一，货物密集堆叠时，底层或相邻目标会被遮挡，容易漏检；第二，箱体外观和纹理高度相似，实例边界容易粘连，导致掩膜不完整；第三，盘点任务不只是“看见几个目标”，还需要根据顶层和下层结构推理真实数量。因此，如果只使用原始 YOLO11s-Seg，难以满足复杂仓储场景下的高精度盘点需求。

## Slide 5 本文提出 YOLO11-StackLite（约 45 秒）

针对上述问题，本文以 YOLO11-Seg 为基线，提出了 YOLO11-StackLite，也就是面向堆叠仓储场景的轻量化实例分割网络。整体改进主要从三个维度展开。  

第一，设计通道拆分注意力模块 C2SASA，用来强化关键区域建模，使网络更关注货物边界、角点和层间交界区域。第二，引入重排分组卷积 SSConv，用更轻量的方式完成特征下采样，在降低计算量的同时尽量保留局部结构信息。第三，提出 C3k2_QDConv 四向卷积模块，通过水平、垂直等方向性特征表达，增强模型对粘连边界的区分能力。通过这些改进，模型能够更好适应遮挡严重、纹理重复的堆叠货物场景。

## Slide 6 关键模块设计（约 60 秒）

这一页展示三个关键模块的具体作用。首先是 C2SASA，也就是通道拆分注意力模块。它不是对所有通道都做复杂注意力增强，而是只对部分通道执行选择性激活，从而在控制计算量的同时，突出与货物结构相关的关键特征，减少重复纹理和背景干扰。  

第二是 SSConv 重排分组卷积模块。它通过通道拆分、分组卷积、逐点卷积以及标准卷积分支的组合，降低 Neck 下采样阶段的计算开销，同时尽量保留箱体边界、角点和局部几何结构。  

第三是 C3k2_QDConv 四向卷积模块。堆叠货物通常具有比较规则的水平和垂直边界，因此本文引入非对称卷积和位移分支，强化对规则箱体边缘和层间分界线的捕捉。这样可以减少掩膜粘连和边界缺损，使实例分割结果更利于后续计数。

## Slide 7 后处理计数算法的具体实现（约 60 秒）

实例分割得到目标掩膜之后，还需要进一步完成数量推理。因为在立体堆叠场景中，可见目标数量并不一定等于真实库存数量，尤其是底层货物会被上层遮挡。  

本文将图像状态分为三类：空货物、顶层满载和顶层未满载。对于空货物图像，如果没有检测到有效实例，就直接判定为空；对于顶层满载图像，因为缺乏完整顶层结构信息，系统主要根据下层区域进行直接计数或保守处理；对于顶层未满载图像，同时包含 up_box 和 down_box，具备比较完整的层次结构信息，因此可以进入面积比估计流程。  

具体来说，本文基于目标掩膜的凸包点和关键点，估计不同层之间的可视面积比例，再结合顶层是否满载进行数量推理。这样就把“分割结果”转化成了“盘点数量”，提升了方法的任务适用性和可解释性。

## Slide 8 面向边缘部署的轻量化压缩路线（约 45 秒）

在完成高精度模型设计后，下一步需要考虑实际部署。仓储现场的边缘服务器通常算力和显存有限，如果模型参数量和 GFLOPs 过高，会影响实时性和部署成本。  

因此本文设计了一条任务导向的压缩路线：首先训练教师模型，然后进行稀疏训练，使不重要通道逐渐显现；接着进行结构化裁剪，得到真实紧凑的网络结构，而不是只产生稀疏权重；随后通过微调恢复精度，最后利用知识蒸馏进一步增强压缩模型。压缩目标不仅是降低 Params 和 GFLOPs，还要尽量保持 box mAP、mask mAP、DA 和 CA 等任务指标稳定。

## Slide 9 剪枝与蒸馏公式（约 55 秒）

这一页给出了剪枝和蒸馏的关键公式。剪枝部分使用 BN 层的缩放因子来衡量通道重要性，也就是这里的 \(s_l^i = |\gamma_l^i|\)。缩放因子越小，说明该通道对特征表达的贡献相对越弱，更适合作为剪枝候选。  

但本文模型包含 C2SASA、SSConv 和 C3k2_QDConv 等自定义结构，不能简单按单个通道独立裁剪，否则可能破坏分支对齐关系。因此本文加入通道对齐约束，例如 C3k2_QDConv 需要保持四向分支同步，SSConv 需要满足二分支和组卷积约束，C2SASA 需要保证注意力头维度合法。  

蒸馏部分则通过软目标蒸馏和多阶段特征蒸馏，让压缩后的学生模型学习教师模型在复杂堆叠场景中的判断逻辑，从而尽量恢复剪枝带来的精度损失。

## Slide 10 实验数据集与验证方法（约 45 秒）

实验方面，本文基于真实烟草仓储场景整理形成了 TCSD 数据集，共 794 张有效图像，图像分辨率为 1280×960。标注类别包括 up_box，也就是顶层单箱体，以及 down_box，也就是非顶层层级区域。  

数据状态分为三类：空货物图像用于状态分流，顶层满载图像主要用于直接计数，顶层未满载图像同时包含上层和下层结构，是面积比估计的关键样本。为了保证评估结果稳定可靠，实验采用五折交叉验证。实验环境为 i7-13620H、RTX 4090、PyTorch 2.3.0 和 CUDA 12.1。

## Slide 11 方法性能的评价指标（约 35 秒）

本文的评价指标不仅关注常规实例分割性能，也关注最终盘点任务是否正确。  

其中，box mAP 衡量检测框定位能力，mask mAP 衡量掩膜轮廓质量；Params 和 GFLOPs 分别衡量模型存储开销和理论计算复杂度。除此之外，本文还引入 DA 和 CA 两个任务指标。DA 表示检测准确率，用来判断结构识别是否正确；CA 表示盘点准确率，用来判断最终数量是否正确。也就是说，本文不仅看模型“分得准不准”，更关心分割结果能否真正转化为正确的库存数量。

## Slide 12 YOLO11-StackLite 各模块消融实验（约 60 秒）

这一页是 YOLO11-StackLite 的模块消融实验结果。可以看到，单独加入 C2SASA、SSConv 或 C3k2_QDConv 时，模型的 box mAP 和 mask mAP 相比基线都有一定提升，说明三个模块都能独立带来收益。  

当三个模块同时加入后，模型取得了最优综合表现，平均 box mAP 达到 85.7%，平均 mask mAP 达到 84.7%。同时，模型参数量从基线的 10.1M 降至 9.4M，GFLOPs 从 35.5 降至 34.9。  

这说明本文的改进不是单纯用更复杂的结构换取精度，而是在提高分割性能的同时，也降低了模型复杂度。三个模块之间形成了协同作用，既提升了复杂场景下的目标识别能力，也为后续边缘部署打下基础。

## Slide 13 不同实例分割模型的性能对比（约 55 秒）

这一页展示了本文方法与其他实例分割模型在 TCSD 数据集上的对比结果。从五折交叉验证的 mAP 指标来看，YOLO11-StackLite 在保持较低复杂度的前提下，取得了较好的分割精度。  

更重要的是，在检测与盘点任务指标上，YOLO11-StackLite 的平均 DA 达到 95.88%，平均 CA 达到 94.87%，均高于其他对比方法。这说明本文模型的优势不仅体现在检测框和掩膜层面的精度提升，也体现在最终库存盘点任务中。  

换句话说，YOLO11-StackLite 在精度、效率和任务可用性之间取得了较好的平衡，更适合立体仓库堆叠货物盘点这一实际应用场景。

## Slide 14 实例分割可视化效果对比（约 45 秒）

这一页是原有方法 YOLO11s-Seg 与新方法 YOLO11-StackLite 的可视化对比。可以看到，在复杂堆叠区域，原始模型更容易出现漏检、低置信度目标被过滤，以及掩膜边界不完整等问题。  

相比之下，YOLO11-StackLite 对密集堆叠货物的边界保持更完整，部分低置信度但有效的目标也能被正确保留，漏检情况明显减少。这与前面提出的 C2SASA 关键区域建模和 C3k2_QDConv 方向性边界增强是一致的。可视化结果进一步说明，本文模型对遮挡和粘连场景具有更强的适应能力。

## Slide 15 不同装载状态下 DA 与 CA（约 55 秒）

这一页进一步分析不同装载状态下的检测准确率和盘点计数准确率。对于全量样本，YOLO11-StackLite 的 DA 达到 95.88%，CA 达到 94.87%，说明整体盘点流程具有较高稳定性。  

对于顶层未满载这一困难子集，平均 DA 为 83.49%，平均 CA 为 80.90%。这个子集难度更高，因为它不仅需要检测和分割目标，还需要基于上层与下层结构进行面积比推理。虽然指标低于全量样本，但它更能体现后处理计数方法的价值。  

从结果来看，本文方法能够在复杂结构图像中完成较可靠的数量推断，同时对于视觉证据不足的情况，论文中也采用保守回退机制，避免盲目输出不可靠结果。

## Slide 16 蒸馏恢复与压缩实验结果（约 60 秒）

这一页展示压缩实验结果。原始 YOLO11-StackLite 的参数量为 9.4M，GFLOPs 为 34.9；经过结构化剪枝后，压缩模型参数量降至 7.8M，GFLOPs 降至 29.1。  

从性能恢复过程来看，Pruned Init 直接剪枝后精度有所下降；经过微调恢复后，box mAP、mask mAP、DA 和 CA 都明显回升；进一步加入知识蒸馏后，平均 DA 达到 95.10%，平均 CA 达到 94.02%，相比原模型只出现小幅回落。  

推理速度方面，压缩蒸馏模型的延迟由 18.1 ms 降至 16.2 ms，FPS 从 55.24 提升到 61.72。结果说明，本文压缩路线能够在降低模型复杂度的同时，较好保持盘点任务性能，具备面向边缘部署的应用潜力。

## Slide 17 创新之处与未来工作（约 55 秒）

最后总结本文的创新之处。第一，提出 YOLO11-StackLite，通过 C2SASA、SSConv 和 C3k2_QDConv 提升复杂堆叠场景下的实例分割能力。第二，构建面积比后处理计数流程，实现从实例分割结果到库存盘点数量的转换，提高了方法的任务适用性和可解释性。第三，设计结构化剪枝与知识蒸馏结合的压缩路线，在保持主要任务性能的同时降低模型复杂度，提高了边缘部署适配性。  

未来工作主要包括三个方向：一是扩展更多仓储场景、光照条件和货物类型的数据，提升泛化能力；二是引入多视角、深度或三维信息，增强复杂遮挡场景下的鲁棒性；三是在 Jetson Xavier NX、RK3588 等边缘设备上进行实机部署测试，进一步验证模型的实际运行效果。

## Slide 18 致谢（约 15 秒）

以上就是我的汇报内容。感谢各位老师的聆听，也感谢导师和课题组同学在论文研究与实验过程中给予的指导和帮助。恳请各位老师批评指正。

